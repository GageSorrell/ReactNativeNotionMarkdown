appId: com.gagesorrell.markdown.storybook
---
# Shows the default level-1 heading; cycling `level` needs the Controls addon (deferred).
- runFlow:
    file: ../_shared/open-story.yaml
    env:
      STORY_ID: "renderer-blocks--heading"
      ASSERT_TEXT: "Heading text"
